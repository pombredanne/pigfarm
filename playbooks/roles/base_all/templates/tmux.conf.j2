#Conflicts with Vim
#unbind C-b
#set -g prefix C-a
set -g default-terminal "screen-256color"
set-window-option -g mode-keys vi

#Make hjkl keys navigate tabs
unbind-key j
bind-key j select-pane -D
unbind-key k
bind-key k select-pane -U
unbind-key h
bind-key h select-pane -L
unbind-key l
bind-key l select-pane -R

set -g mouse on

#new  -n WeeChat /usr/bin/weechat-curses
#new  -n WeeChat /bin/false
new  -n Shells
#splitw -h
#neww -n IDE 
#splitw -h 
#splitw -v -t 1
