---
- fail: msg="This role requires Fedora >=24"
  when: >
    ansible_os_family != 'RedHat' or
    ansible_distribution != 'Fedora' or
    ansible_distribution_major_version|int < 24

- include: budrys.yml
  when: inventory_hostname == 'budrys'
  tags: multiseat

- meta: flush_handlers
